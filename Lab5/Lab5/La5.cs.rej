diff a/Lab5/Lab5/La5.cs b/Lab5/Lab5/La5.cs	(rejected hunks)
@@ -9,34 +9,35 @@
 
             for (int i = 0; i < usersPerDay.Length; i++)
             {
-                uint u = usersPerDay[i]; 
-            }
+                uint u = usersPerDay[i];
 
-            for (int j = 0; j < revenuePerDay.Length; j++)
-            {
-                double R = Math.Round(revenuePerDay[j], 2);
+                for (int j = 0; j < revenuePerDay.Length; j++)
+                {
+                    double R = Math.Round(revenuePerDay[j], 2);
+                    if (0 <= u && u <= 10)
+                    {
+                        R = u / 2;
+                        return true;
+                    }
+                    else if (10 < u && u <= 100)
+                    {
+                        R = 16 * u / 5 - 27;
+                        return true;
+                    }
+                    else if (100 < u && u <= 1000)
+                    {
+                        R = u ^ 2 / 4 - 2 * u - 2007;
+                        return true;
+                    }
+                    else if (u > 1000)
+                    {
+                        R = 245743 + u / 4;
+                        return true;
+                    }
+                }
             }
 
-            if (0 <= usersPerDay[] && u <= 10)
-            {
-                R = u / 2;
-                return true;
-            }
-            else if (10 < u && u <= 100)
-            {
-                R = 16 * u / 5 - 27;
-                return true;
-            }
-            else if (100 < u && u <= 1000)
-            {
-                R = u ^ 2 / 4 - 2 * u - 2007;
-                return true;
-            }
-            else if (u > 1000)
-            {
-                R = 245743 + u / 4;
-                return true;
-            }
+
             if (usersPerDay.Length != revenuePerDay.Length)
             return false;
 
